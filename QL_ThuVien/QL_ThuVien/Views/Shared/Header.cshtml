<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Quản Lý Thư Viện", "Index", "ListBook", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">

                <li>
                    <a href="@Url.Action("Index","ListBook")">Danh sách các sách</a>
                </li>
            </ul>
            <ul class="nav navbar-nav">

                <li>
                    <a href="@Url.Action("TimKiemTheoID","TraCuuPhieuMuon")">Tra cứu phiếu mượn</a>
                </li>
            </ul>



            <ul class="nav navbar-nav navbar-right">
                <li>
                    <img src="@Url.Content("~/Content/Image/gio.png")" style="height:40px;width:40px" />
                    @Html.Action("BagBook", "GioSach");
                </li>
                <li>
                    <a href="@Url.Action("Index","Home")">LogOut</a>
                </li>
                <li class="format-hello">
                    @if (Session["NameUser"] == null)
                    {
                        <span>Hello</span>
                    }
                    else
                    {
                        <span>Hello @Session["NameUser"]</span>
                    }
                </li>
            </ul>
        </div>
    </div>
</div>
